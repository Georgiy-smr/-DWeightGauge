<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:vm="clr-namespace:IPS_CALC.VIewModels"
                    xmlns:v="clr-namespace:IPS_CALC.Veiws">

    <DataTemplate DataType="{x:Type vm:IpsViewModel}">
        <v:IPsView DataContext="{Binding}"/>
    </DataTemplate>
    <DataTemplate DataType="{x:Type vm:CargoViewModel}">
        <v:IPsView DataContext="{Binding}"/>
    </DataTemplate>
</ResourceDictionary>